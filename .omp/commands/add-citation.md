---
description: Find, acquire, and add a BibTeX entry to main.bib
---

Add a citation for: $ARGUMENTS

Use `skill://searching-literature` to locate the work (OpenAlex/Crossref lead) and
`skill://retrieving-paper-pdfs` to acquire it, then format the entry per
`skill://formatting-bibtex-entries` and append it to `main.bib` only if no entry with the same
DOI/title already exists.
